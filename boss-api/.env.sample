# Set to the absolute path of the Source of Truth repository if running outside the repo
# Defaults to the repository root when unset
SOT_PATH=

# Optional: override the port (defaults to 4000)
# PORT=4000
