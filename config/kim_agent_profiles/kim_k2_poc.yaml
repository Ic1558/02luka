id: kim_k2_poc
name: "Kim K2 Proof-of-Concept"
description: |
  Routes Telegram conversations through the Kimi K2 Thinking model on OpenRouter.
  Designed for side-by-side evaluation against the default Kim profile.
provider: k2_thinking
channel: kim:requests
metadata:
  provider_config: agents/kim_bot/providers/k2_thinking.yaml
  request_mode: "openrouter:kimi/k2"
  notes: "Parallel profile – must be selected explicitly or via /k2 command."
