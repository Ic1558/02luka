{
  "$id": "file:config/schemas/delegation_watchdog.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Delegation Watchdog Config",
  "type": "object",
  "properties": {
    "stuck_timeout_ms": { "type": "integer", "minimum": 1000 },
    "max_pending":      { "type": "integer", "minimum": 0 },
    "inbox":            { "type": "string",  "minLength": 1 },
    "history":          { "type": "string",  "minLength": 1 }
  },
  "required": ["inbox","history"],
  "additionalProperties": true
}
