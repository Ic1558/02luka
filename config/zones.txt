# 02luka Repository Zones
# Format: One zone per line (directory name)
# See docs/REPOSITORY_STRUCTURE.md for full documentation

# Core Zones (Primary Organization)
a           # Agent workspace (CLC memory, protocols, logic)
f           # Foundation layer (AI context, bridges)
g           # Global system tools, reports, web assets, LaunchAgents
memory      # Agent memory SOT (per-agent: clc/, gg/, gc/, mary/, paula/)

# Human & Application Zones
boss        # Human workspace (inbox/outbox pattern)
boss-api    # Boss API backend (Node.js)
boss-ui     # Boss UI frontend (Vite/React)

# System Infrastructure
gateway     # API gateway services
run         # Runtime data & logs (not committed)
setup       # Installation & setup scripts

# Development & Documentation
scripts     # Dev/ops utilities, manual tools
docs        # Documentation (user guides, system docs)
config      # Configuration files
contracts   # API contracts & JSON schemas

# Testing & CI
test-results  # Playwright test output (auto-generated)
.github       # GitHub Actions workflows

# Dependencies & Build
node_modules  # npm dependencies (auto-generated)

# Cleanup & Backups
.trash      # Deleted files (reversible cleanup: backup/, temp/, conflict/)

# Deprecated/Legacy (empty directories to be removed)
# output    # Empty - remove during next cleanup
# backups   # Empty - consolidated to .trash/
