# /local — Create & DROP WO to shell (Terminalhandler)
Same schema. Drop to `~/02luka/bridge/inbox/shell/`.
Use `intent: run_shell` and `artifacts[0].type: shell_script` for commands.
Set `target_candidates: [shell]` and `strict_target: true`.
