{
  "check_health": {
    "description": "Query the local Boss API stub to verify service status.",
    "skills": [
      {
        "name": "http_fetch.py",
        "params": {
          "url": "http://localhost:4000/api/status",
          "method": "GET"
        }
      }
    ]
  },
  "deploy_dashboard": {
    "description": "Demonstrate a dashboard deployment through a stubbed workflow.",
    "skills": [
      {
        "name": "ui_emit.py",
        "params": {
          "title": "Dashboard deployment (stub)",
          "notes": "Use this to confirm that automation intents are being routed correctly before invoking real infrastructure scripts."
        }
      }
    ]
  },
  "update_docker_yaml": {
    "description": "Preview docker-compose updates by echoing the intended parameters.",
    "skills": [
      {
        "name": "ui_emit.py",
        "params": {
          "title": "Docker Compose update (stub)",
          "notes": "Review the generated configuration before applying changes to persistent services."
        }
      }
    ]
  }
}
