# 🌍 HOWTO_TWO_WORLDS_v2.md  
## How to Work with the Two Worlds Model (Developer Edition)

**Status:** Active (CLI HOWTO)  
**World:** CLI / Interactive (World 1)  
**Audience:** Boss + Devs + Agent Authors  
**Last Updated:** 2025-12-09  

---

## 0. Scope & References

> **Scope Notice — CLI World Only**

เอกสารนี้คือ **คู่มือการใช้งานสำหรับ "World 1: CLI / Interactive"**  
ใช้เมื่อต้องตอบคำถามว่า:

> "ตอนนี้เราอยู่โลกไหน / แตะ Zone อะไร / ใครมีสิทธิ์เขียน / ต้องกลัวอะไรบ้าง?"

**เอกสารนี้ _ไม่ใช่_ กฎหมายหลัก**  
มันคือ "คู่มือมนุษย์" ที่แปลจากกฎหมายหลักให้ใช้ได้จริงในชีวิตประจำวัน

- 🧠 **Kernel Law (กฎหมายหลัก):**  
  `g/docs/GOVERNANCE_UNIFIED_v5.md`

- 🛠 **Background Law (World 2):**  
  `g/docs/AI_OP_001_v5.md`

- 📚 **Persona / Capability Model:**  
  `g/docs/PERSONA_MODEL_v5.md` ✅

- 🚦 **Mary Router Implementation:**  
  `tools/mary_dispatch.py`

**กฎสำคัญ:**  
ถ้า HOWTO ฉบับนี้ดูเหมือนขัดกับ `GOVERNANCE_UNIFIED_v5.md` →  
ให้ถือว่า **GOVERNANCE_UNIFIED_v5.md เป็น SOT** แล้วปรับ HOWTO ตามมัน

---

## 1. คิดแบบง่ายก่อน: "ตอนนี้เราอยู่โลกไหน?"

ถามตัวเองก่อนทุกครั้งก่อนจะสั่งให้ Agent แก้ไฟล์:

> **"คำสั่งนี้ถูก Trigger โดยใคร?"**

| Feature         | 🏎 World 1: CLI / Interactive | 🚛 World 2: Background / Autonomous |
|----------------|-------------------------------|-------------------------------------|
| Trigger        | Boss / มนุษย์กดเอง (Terminal, Cursor, Antigravity, GMX, Codex, Gemini CLI, LAC CLI) | Cron, LaunchAgent, Watchdog, Queue, Worker, Daemon |
| Power          | **FULL POWER** (Advisory Rules) | **STRICT GOVERNANCE** (Mandatory Rules) |
| Law Reference  | HOWTO v2 + GOVERNANCE_UNIFIED_v5 | AI_OP_001_v5 + GOVERNANCE_UNIFIED_v5 |
| Override       | Boss Override ได้ (ยกเว้น DANGER) | ไม่มี Auto-Override, ต้องเปลี่ยนโค้ด/WO |
| Writer Type    | GMX, Codex, Gemini, LAC, CLS, Liam (เขียน), GG/GM (เสนอ) | CLC/LPE only (เขียน), Workers อื่น = สร้าง WO |
| Speed          | เน้นเร็ว + productive          | เน้นชัวร์ + audit                    |

**Rule of Thumb (จำง่าย ๆ):**

- ถ้า **Boss นั่งอยู่หน้าจอ** แล้วสั่ง → มักจะเป็น World 1  
- ถ้า **ไม่มีคนดูอยู่** แล้วมันรันเอง → มักจะเป็น World 2

> ⚠️ CLI Agent ทุกตัวต้อง "resolve world" ให้ชัดเจน ก่อนคิดจะเขียนไฟล์

---

## 2. Zone Mapping: เรากำลังแตะของประเภทไหน?

หลังรู้แล้วว่าเราอยู่ World ไหน → ถามต่อ:

> **"Path นี้อยู่ Zone อะไร?"**

### 2.1 Zone Types (จำแค่ 3 แบบ)

1. **🔓 OPEN Zone — เล่นได้ค่อนข้างอิสระ**
2. **🔒 LOCKED Zone — ของระบบ / routing / governance**
3. **💣 DANGER Zone — แตะผิด = พัง**

### 2.2 Path → Zone Table (สำหรับ CLI World)

| Zone Type   | ตัวอย่าง Paths                                                                           | ความหมาย (CLI World)                                      |
|-------------|-------------------------------------------------------------------------------------------|------------------------------------------------------------|
| 🔓 OPEN     | `apps/**`, `tools/**`, `agents/**`, `tests/**`, `docs/**` (ยกเว้น governance), `g/reports/**`, `bridge/docs/**`, `bridge/samples/**` | พื้นที่ทำงานปกติของ Dev + Agents                         |
| 🔒 LOCKED   | `core/**`, `launchd/**`, `bridge/core/**`, `bridge/inbox/**`, `bridge/outbox/**`, `bridge/handlers/**`, `bridge/templates/**`, `bridge/production/**`, `g/docs/governance/**` | โครงสร้างระบบ, routing, bridge, กฎหมาย                   |
| 💣 DANGER   | `/`, `/System/**`, `/usr/**`, `/bin/**`, `/sbin/**`, `/etc/**`, `~/.ssh/**`, `rm -rf ~/02luka` | ระบบปฏิบัติการ, key, root delete, command เสี่ยงสูงมาก    |

> 🔍 รายละเอียด formal อยู่ใน `GOVERNANCE_UNIFIED_v5.md` Section 3.1–3.2

---

## 3. Mary Router: ให้คิดว่าเป็น "ไฟจราจร"

ก่อนเขียนไฟล์ ให้คิดว่าเรากำลังผ่าน Mary Router เสมอ:

```text
World + Path  →  Zone  →  Lane
Lane          →  สิทธิในการเขียน

Mary จะคืนค่า Lane กลับมา:
	•	🟢 FAST
	•	🟡 WARN
	•	🔴 STRICT
	•	⛔ BLOCKED
```

### 3.1 FAST Lane (CLI + OPEN)

- **เงื่อนไข:**
  - World = CLI
  - Zone = OPEN
- **Behavior:**
  - ✅ เขียนได้เลย (GMX / Codex / Gemini / LAC / Liam / CLS)
  - ❌ ไม่ต้อง WO
  - ✅ แนะนำ SIP (mktemp → mv) แต่ไม่บังคับเสมอ

**ตัวอย่าง:**
- แก้ `tools/save.sh`
- เพิ่มหน้าใหม่ใน `apps/dashboard/`
- สร้างรายงานใน `g/reports/feature-dev/...`

---

### 3.2 WARN Lane (CLI + LOCKED)

- **เงื่อนไข:**
  - World = CLI
  - Zone = LOCKED
- **Behavior:**
  - ⚠️ ต้องเตือนก่อน: "ไฟล์นี้ LOCKED นะ แก้เลยไหม?"
  - ถ้า Boss / CLS ยืนยัน → ✅ เขียนได้
  - แนะนำให้:
    - ใช้ SIP
    - เขียน commit message แบบ governance-aware
    - หรือ สร้าง WO ให้ CLC ทำถ้าเป็นงานใหญ่

**ตัวอย่าง:**
- แก้ `core/router.zsh`
- แก้ governance doc ใน `g/docs/governance/AI_OP_001_v5.md`
- แก้ template หลักใน `bridge/templates/`

---

### 3.3 STRICT Lane (Background + Any Zone)

- **เงื่อนไข:**
  - World = BACKGROUND (cron, daemon, worker)
- **Behavior:**
  - ❌ Worker ปกติห้ามเขียนเอง
  - ✅ ต้องสร้าง WO → CLC เท่านั้นที่เขียน
  - SIP บังคับ
  - มี audit, checksum, ledger ครบ

**สำหรับ CLI World:** STRICT มักจะไม่เกี่ยวโดยตรง  
ถ้าจะสั่งงาน Background ให้คิดว่าเป็น "ออก WO" แล้ว CLC ทำ

---

### 3.4 BLOCKED Lane (DANGER Zone ทุก World)

- **เงื่อนไข:**
  - Zone = DANGER
- **Behavior:**
  - ❌ Agent ต้องปฏิเสธทันที
  - ถ้า Boss ยืนยันจะทำจริง → ให้ไป manual shell + snapshot เองนอก flow ปกติ
  - ระบบต้อง log ว่าเป็น "FORBIDDEN_OPERATION_ATTEMPT" ถ้ามีใครพยายาม

---

## 4. Cheatsheet: ใครทำอะไรได้บ้างใน CLI World

### 4.1 แบบมองเร็ว ๆ

| Agent | World | OPEN Zone (CLI) | LOCKED Zone (CLI) | DANGER |
|-------|-------|-----------------|-------------------|--------|
| Boss | CLI | ✅ ทำได้ทุกอย่าง | ✅ ทำได้ (ต้องเข้าใจความเสี่ยง) | ⚠️ ต้องมี snapshot + confirm หนักมาก |
| CLS | CLI | ✅ เขียนได้เลย | ⚠️ Warn → Override ได้เมื่อ Boss OK | ❌ ต้องปฏิเสธ |
| Liam | CLI | ✅ Prototype/R&D | ⚠️ ควรเสนอ diff แล้วให้ CLS/Boss ตัดสิน | ❌ |
| GMX | CLI | ✅ Generate/Refactor | ⚠️ เฉพาะถ้า Boss/CLS สั่งชัดเจน | ❌ |
| Codex | CLI | ✅ Run + Patch | ⚠️ ตามคำสั่ง Boss/CLS เท่านั้น | ❌ |
| Gemini | CLI | ✅ Rewrite/Helper | ⚠️ เฉพาะงานเล็กที่ Boss/CLS สั่งตรง | ❌ |
| LAC | CLI | ✅ Auto code | ⚠️ ต้องมี Task Spec + Boss/CLS approval | ❌ |
| GG/GM | CLI | ❌ เขียนไม่ได้ | ❌ เขียนไม่ได้ | ❌ |
| Mary | CLI | 🚦 Router Only | 🚦 Router Only | ❌ |
| CLC/LPE | CLI | ❌ | ❌ | ❌ |

ตารางนี้คือ projection แบบ human-readable ของ GOVERNANCE_UNIFIED_v5 Section 4.2.1

---

## 5. Decision Flow (แบบใช้ได้จริงในหัว)

ให้คิดเป็น 3 Step ง่าย ๆ:

**STEP 1:** ใครสั่ง? → World  
**STEP 2:** แตะไฟล์อะไร? → Zone  
**STEP 3:** Mary ให้เลนอะไร? → Lane → ทำ / เตือน / บล็อค / ออก WO

### 5.1 Pseudo Flow

1. **Trigger Check**
   - ถ้า Boss/Dev สั่งจาก Terminal/Cursor → World = CLI
   - ถ้า script รันเอง (cron/LaunchAgent/worker) → World = BACKGROUND

2. **Zone Check (จาก path)**
   - ถ้า DANGER pattern → Zone = DANGER → Lane = BLOCKED
   - else if LOCKED pattern → Zone = LOCKED
   - else → Zone = OPEN

3. **Lane Resolution (Mary)**
   - if World=CLI and Zone=OPEN   → FAST
   - if World=CLI and Zone=LOCKED → WARN
   - if World=BACKGROUND          → STRICT (CLC only)
   - if Zone=DANGER               → BLOCKED

4. **Action**
   - FAST   → ทำเลย, แต่อย่าลืมระวัง
   - WARN   → เตือน Boss, ถ้า OK → ทำ, ถ้าไม่ OK → เขียน WO
   - STRICT → สร้าง WO → รอ CLC
   - BLOCKED→ ปฏิเสธ, เสนอ snapshot/manual ถ้า Boss ดันจะทำจริง

---

## 6. FAQ — ใช้งานจริงในชีวิตประจำวัน

### Q1: "Boss อยากใช้ GMX แก้ tools/save.sh ได้ไหม?"

- **World:** CLI (Boss สั่งผ่าน GMX)
- **Path:** `tools/save.sh` → OPEN
- **Lane:** FAST

✅ **ทำได้เลย ไม่ต้อง WO**

แนะนำให้ใช้ SIP ถ้าเป็นการเขียนผ่านสคริปต์

---

### Q2: "แก้ core/router.zsh ผ่าน Cursor ตรง ๆ ผิดกฎไหม?"

- **World:** CLI
- **Path:** `core/router.zsh` → LOCKED
- **Lane:** WARN

**Behavior ที่ถูกต้อง:**

1. Agent ต้องเตือน: "ไฟล์นี้ LOCKED, มีผลต่อ routing ทั้งระบบ"
2. ถ้า Boss/CLS ยืนยัน Override ตอนนี้เลย:
   - ✅ ทำได้ (CLS สามารถ authorize ได้ในฐานะ Boss Proxy)
   - ควร:
     - ใช้ SIP
     - เขียน commit message ให้ชัดว่าเป็น governance/system change
3. ถ้า Boss อยากทำแบบช้าแต่ชัวร์:
   - เขียน WO → ให้ CLC ทำใน Background

---

### Q3: "Cron job ที่รัน Worker อยากแก้ apps/web/config.json เองได้ไหม?"

- **World:** BACKGROUND
- **Path:** `apps/web/config.json` → OPEN
- **Lane:** STRICT

❌ **Worker ห้ามเขียนเอง**

✅ **ต้องสร้าง WO → ให้ CLC ทำตาม AI_OP_001_v5**

---

### Q4: "LAC อยากสร้างไฟล์ใหม่ใน tools/ สำหรับ CLI helper?"

- **World:** CLI
- **Path:** `tools/**` → OPEN
- **Lane:** FAST

✅ **ให้ LAC เขียนได้เลย (Autonomous Code)**

❗ **แต่ควร:**
- Log / commit ให้ชัดว่าเป็น `[LAC]`
- ไม่แตะ LOCKED Zone

---

### Q5: "Boss สั่ง rm -rf ~/02luka ผ่าน Codex CLI ได้ไหม?"

- **Path:** DANGER
- **Lane:** BLOCKED

**ทุก Agent ต้อง:**
- ❌ ปฏิเสธคำสั่งนี้
- ✅ เสนอ:
  - ถ้า Boss ต้องการทำจริง:
    - ต้อง snapshot / backup ก่อน
    - ควรทำ manual shell ด้วยตัวเอง (นอก agent governance ปกติ)

---

### Q6: "Gemini อยากแก้ g/docs/AI_OP_001_v5.md ให้สั้นลงหน่อย?"

- **World:** CLI (ถ้า Boss สั่งผ่าน Gemini)
- **Path:** `g/docs/AI_OP_001_v5.md` → LOCKED (governance)
- **Lane:** WARN

**Behavior:**

1. เตือนว่าเป็น กฎหมายสำคัญ (Background World Law)
2. ถ้า Boss ยืนยันแก้ทันที:
   - ✅ ทำได้
   - แนะนำให้:
     - ใช้ SIP
     - ทำ commit แยก
3. หรือให้เขียน draft แยกไปที่ `g/docs/_drafts/` แล้วค่อย merge ทีหลัง

---

## 7. Minimal Habits สำหรับ Boss & Dev

### 7.1 สำหรับ Boss

1. **ก่อนพิมพ์คำสั่งเสมอ** → คิดสามอย่าง:
   - อยู่ CLI หรือ Background?
   - แตะ OPEN หรือ LOCKED หรือ DANGER?
   - ถ้า FAIL → พังแค่ feature หรือพังทั้งระบบ?

2. **เวลาแก้ LOCKED Zone ผ่าน Cursor / GMX:**
   - ให้ Agent เตือนก่อน
   - ถ้าใจไม่ 100% แน่ → บอกให้มันเสนอ "WO Plan" แทน

3. **เวลาเห็น Agent ปฏิเสธ DANGER:**
   - ถือว่าเป็น behavior ที่ถูกต้องแล้ว
   - ถ้าจะทำจริง → ทำ manual, พร้อม snapshot

---

### 7.2 สำหรับ Dev / Agent Author

1. **อย่า Hardcode กฎเองใน Agent**
   - ให้ถาม Mary Router / อ่าน GOVERNANCE_UNIFIED_v5

2. **ทุก CLI Worker ที่ "เขียนไฟล์" ต้องระบุ:**
   - World สมมติของมัน (CLI / BG)
   - Zone ที่มันแตะ
   - Lane ที่มัน assume

3. **อย่าปล่อย Background Worker เขียนไฟล์โดยตรง**
   - ให้สร้าง WO → ใช้ CLC ตาม AI_OP_001_v5

---

## 8. Quick Reference Block (ให้ Agent แปะไปใช้)

### 8.1 Minimal World/Zone/Lane Logic

```text
# Resolve World
IF trigger in {Terminal, Cursor, Antigravity, GMX CLI, Codex CLI, Gemini CLI, LAC CLI}
   → WORLD = CLI
ELSE
   → WORLD = BACKGROUND

# Resolve Zone
IF path matches DANGER patterns
   → ZONE = DANGER
ELSE IF path matches LOCKED patterns
   → ZONE = LOCKED
ELSE
   → ZONE = OPEN

# Resolve Lane
IF ZONE = DANGER
   → LANE = BLOCKED
ELSE IF WORLD = CLI AND ZONE = OPEN
   → LANE = FAST
ELSE IF WORLD = CLI AND ZONE = LOCKED
   → LANE = WARN
ELSE IF WORLD = BACKGROUND
   → LANE = STRICT
```

---

## 9. ความสัมพันธ์กับเอกสารอื่น

- **GOVERNANCE_UNIFIED_v5.md**
  - คือ กฎหมายหลัก อธิบาย Worlds, Zones, Lanes, Routing, Invariants
  - HOWTO v2 = Projection แบบมนุษย์อ่านง่ายของกฎหมายนี้

- **AI_OP_001_v5.md**
  - ใช้เมื่อ World = BACKGROUND
  - ระบุว่า CLC, LPE, Workers ต้องทำงานยังไง, WO, SIP, Drift

- **PERSONA_MODEL_v5.md**
  - ระบุว่าแต่ละ Persona มี capability ยังไงในแต่ละ World + Zone

**ถ้า Agent สงสัย** → ให้ refer ตามลำดับ:

1. `GOVERNANCE_UNIFIED_v5.md`
2. `AI_OP_001_v5.md` (ถ้าเป็น Background)
3. `HOWTO_TWO_WORLDS_v2.md` (ถ้าเป็น CLI Human HOWTO)

---

**End of HOWTO_TWO_WORLDS_v2.md**

