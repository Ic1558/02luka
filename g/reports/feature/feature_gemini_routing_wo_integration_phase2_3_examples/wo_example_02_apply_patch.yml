# WO Example 2: Small Code Patch
# This WO applies a non-destructive patch to add a comment to a script.
# The patch is inline and follows standard diff format.
wo_id: "wo_gemini_dryrun_002"
origin: "gemini"
created_at: "2025-11-19T10:05:00Z"
intent: "apply_patch"
target_inbox: "bridge/inbox/DRY_RUN/gemini"

metadata:
  dry_run: true
  source_request: "User requested to add a comment clarifying the purpose of the agent_listener script."
  schema_version: "1.0"

payload:
  file_path: "agent_listener.py"
  patch_format: "diff"
  patch_content: |
    --- a/agent_listener.py
    +++ b/agent_listener.py
    @@ -1,3 +1,4 @@
     import os
     
     # This script is responsible for listening to agent-related events.
     
     def main():
         print("Listening for agent events...")
