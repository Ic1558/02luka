# syntax=docker/dockerfile:1
FROM node:20-alpine

WORKDIR /app/g/tools/services

# Pre-install redis module (so it's baked into the image)
RUN npm init -y && npm install --omit=dev redis@^4

# Default command (can be overridden by docker run)
CMD ["node"]
