# Production Ops Health Cron Configuration
# Add these lines to your crontab: crontab -e

# Auto-verify after every boot (5 minutes after boot)
@reboot cd /workspaces/02luka-repo && make verify-ops-publish >> g/logs/ops_verify.log 2>&1

# Continuous 5-minute loop (optional - uncomment if you want ongoing status)
# */5 * * * * cd /workspaces/02luka-repo && make verify-ops-publish >/dev/null 2>&1

# Daily health report (optional - uncomment if you want daily summaries)
# 0 9 * * * cd /workspaces/02luka-repo && make verify-ops-publish >> g/logs/ops_verify_daily.log 2>&1

# Weekly system health check (optional - uncomment if you want weekly deep checks)
# 0 2 * * 0 cd /workspaces/02luka-repo && make verify-ops-publish >> g/logs/ops_verify_weekly.log 2>&1
